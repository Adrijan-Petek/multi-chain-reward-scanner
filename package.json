{
  "name": "multi-chain-reward-scanner",
  "version": "1.0.0",
  "description": "Multi-chain daily reward/event scanner (Base, Optimism, Arbitrum)",
  "main": "src/scan.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "scan:once": "node src/scan.js --once",
    "scan:dry": "node src/scan.js --once --offline --print",
    "start": "node src/scan.js",
    "dev": "node src/scan.js --watch"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "ethers": "^6.10.0",
    "node-fetch": "^3.3.2"
  }
}
